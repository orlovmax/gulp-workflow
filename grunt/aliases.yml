default:
    description: 'Default dev task with static server'
    tasks:
        - 'browserSync:dev'
        - 'watch'


dev:
    description: 'Internal task: process changed files during the watch task'
    tasks:
        - 'newer:coffee'
        - 'newer:concat'
        - 'newer:sass'
        - 'newer:stylus'
        - 'newer:jade'
        - 'sync'


start:
    description: 'Move bower dependencies to main dev folders'
    tasks:
        - 'shell:bower'
        - 'bower'
        - 'clean:gitkeep'


regenerate:
    description: 'Regenerate project by running all tasks without newer directive'
    tasks:
        - 'coffee'
        - 'concat'
        - 'sass'
        - 'stylus'
        - 'jade'
        - 'sync'
        - 'clean:build'


build:
    description: 'Default build task'
    tasks:
        - 'imagemin'
        - 'processhtml'
        - 'cmq'
        - 'autoprefixer'
        - 'csscomb'
        - 'uglify'
        - 'csso'
        - 'htmlmin'
        - 'clean:dev'
        - 'cacheBust'
        - 'browserSync:build'


deploy:
    description: 'Commit build folder and push changes to remote'
    tasks:
        - 'shell:deploy'

sprite:
    description: 'Sprite creation task. Should be configured before running'
    tasks:
        - 'sprite'
