default:
    tasks:
        - 'newer:coffee'
        - 'newer:concat'
        - 'newer:sass'
        - 'newer:less'
        - 'newer:stylus'
        - 'newer:jade'
        - 'newer:imagemin'
        - 'sync'
        - 'watch'


bower-dev:
    tasks:
        - 'bower_concat'
        # - 'bowercopy'
        - 'clean:gitkeep'
        - 'clean:bower'


regen:
    tasks:
        - 'coffee'
        - 'concat'
        - 'sass'
        - 'less'
        - 'stylus'
        - 'jade'
        - 'sync'
        - 'clean:build'


build:
    tasks:
        - 'processhtml'
        - 'cmq'
        - 'autoprefixer'
        - 'csscomb'
        - 'uglify'
        - 'csso'
        - 'htmlmin'
        - 'clean:debug'
        - 'cacheBust'


deploy:
    tasks:
        - 'shell:deploy'

build-deploy:
    tasks:
        - 'shell:buildDeploy'

zip-all:
    tasks:
        - 'compress:all'

server:
    tasks:
        - 'browserSync'
