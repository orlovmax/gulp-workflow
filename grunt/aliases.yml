default:
    description: 'Default dev task'
    tasks:
        - 'newer:coffee'
        - 'newer:concat'
        - 'newer:sass'
        - 'newer:less'
        - 'newer:stylus'
        - 'newer:jade'
        # - 'newer:imagemin'
        - 'sync'
        - 'watch'


bower-dev:
    description: 'Move bower dependencies to main dev folders'
    tasks:
        - 'bower'
        - 'clean:gitkeep'


regen:
    description: 'Regenerate project by running all tasks without newer directive'
    tasks:
        - 'coffee'
        - 'concat'
        - 'sass'
        - 'less'
        - 'stylus'
        - 'jade'
        - 'imagemin'
        - 'sync'
        - 'clean:build'


build:
    description: 'Default build task'
    tasks:
        - 'processhtml'
        - 'cmq'
        - 'autoprefixer'
        - 'csscomb'
        - 'uglify'
        - 'csso'
        - 'htmlmin'
        - 'clean:dev'
        # - 'cacheBust'


deploy:
    description: 'Commit build folder and push changes to remote'
    tasks:
        - 'shell:deploy'

zip:
    description: 'Creates archive with static build'
    tasks:
        - 'compress:build'

server:
    description: 'Run server for static build'
    tasks:
        - 'browserSync'

sprite:
    description: 'Sprite creation task. Should be configured before running'
    tasks:
        - 'sprite'
